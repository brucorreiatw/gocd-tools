spec:
  data:
    app:
      name: example-app
    configmap:
      data: 'ENV: dev'
    image:
      repository: nexus.com.br
      tag: 1.0.6
    resources:
      limits:
        cpu: 512m
      requests:
        cpu: 256m
  enable: true
  ingress:
    additional:
      path: /example-app
